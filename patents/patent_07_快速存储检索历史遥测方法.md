# 发明/实用新型专利申请技术交底书

| 发明人/设计人基本资料 | |
|---|---|
| **第一发明人/设计人** | |
| 姓名（必填） | （待填写） |
| 身份证号、国籍（必填） | （待填写） |
| 电话 | （待填写） |
| 其它联系方式 | （待填写） |
| **第二发明人/设计人** | |
| 姓名 | （待填写） |
| 电话 | （待填写） |
| **申请人基本资料** | |
| 第一申请人公司名称（必填） | （待填写） |
| 地址（邮编）（必填） | （待填写） |
| 组织机构代码/身份证号（必填） | （待填写） |

**所属项目（必填）：** 低轨卫星星务平台历史遥测存储与检索技术研究

**专利编号（由专利部门给出）：** （待填写）

---

## 说明书摘要

本发明公开一种基于北斗星时绝对定位的历史遥测快速存储检索方法，应用于低轨卫星星务平台。所述方法在星务平台创建n个顺序编号的历史遥测文件，每个文件容纳m条定长历史遥测记录；每条记录由北斗星时秒数时间戳、定长遥测项数据区及定长遥测项掩码构成；历史遥测记录的存储槽位由北斗星时秒数经采集周期整除后唯一确定，写入与按时间段检索均不依赖当前写地址或写指针元数据；系统崩溃复位后直接依据当前北斗星时重新确定存储槽位并继续写入。本发明解决了相对寻址方案在复位后须恢复写指针的技术问题，实现绝对时标驱动的可靠存储与快速检索。

---

## 一、发明/实用新型名称

**名称：一种基于北斗星时绝对定位的历史遥测快速存储检索方法**

---

## 二、所属技术领域

本发明涉及航天卫星星务管理技术领域，特别是涉及一种面向低轨卫星星务平台、以北斗星时绝对确定存储槽位的历史遥测定长存储与按时间段检索方法。

---

## 三、现有技术（背景技术）

**背景技术引证文件：**

1. 中国发明专利 CN115664490B，发明名称：一种低轨卫星遥测数据存储及回放系统及其方法；
2. 环形缓冲区及写指针管理的通用存储组织方法（航天器星务软件工程实践中的常规技术手段）。

**背景技术：**

低轨卫星星务平台在轨运行过程中，持续采集平台各分系统及有效载荷的遥测参数，形成历史遥测数据。历史遥测数据是地面开展故障追溯、在轨异常分析、寿命评估及健康管理的基础数据。受测控弧段时长与下行带宽限制，地面无法在全部时刻获得完整实时遥测，星上必须在非易失性存储介质中长期保存一定时间跨度的历史遥测，供遥控调取、延时下传或星上自主分析使用。

**（一）最接近的现有技术及其不足**

中国发明专利CN115664490B公开了一种低轨卫星遥测数据存储及回放系统及其方法：在测控管理单元设置FLASH存储器实时存储遥测数据；回放时依据当前星上时间Tsd、当前存储地址ADsd以及恒定存储速率，按公式

ADstart＝ADsd−(Tsd−Tqs)×速率， ADend＝ADsd−(Tsd−Tzz)×速率

计算回放起止地址，从而按指定时间段回放。该方案属于“相对寻址”：回放地址的计算必须以**当前写地址ADsd**为基准。由此带来以下固有缺陷：

（A1）系统必须持续正确维护当前写地址ADsd；ADsd保存在易失性内存或独立元数据区时，星务计算机崩溃复位、看门狗重启或电源切换后，若ADsd丢失或损坏，则无法继续正确计算写入位置与回放地址；

（A2）复位后为重建ADsd，须扫描存储区或回读元数据，耗时长且在介质局部失效时存在重建失败风险；

（A3）写入过程与回放过程均绑定“当前写指针状态”，存储组织仍未脱离写指针依赖，可靠性受写指针一致性制约。

**（二）其他现有技术及其不足**

**1. 索引表／写指针表组织方式**

现有星务软件多采用“数据文件＋索引文件”或“环形缓冲区＋写指针表”。写入时需维护当前文件号、当前写偏移、有效记录数、最早／最晚有效时间等状态，并同步更新索引。索引与数据区一旦不一致，即导致记录不可检索或检索错误。

**2. 按时间戳查找定位**

部分方案在记录中保存时间戳，查询时对文件或索引执行线性查找或二分查找。查找复杂度随记录规模增长，难以满足星上遥控查询的时限要求。

**3. 变长记录存储**

为节省空间采用变长或压缩记录时，无法由记录序号直接换算字节偏移，必须依赖偏移表或链表；偏移表损坏将导致后续记录无法定位。

**4. 遥测项有效性表达不清**

采集周期内部分遥测项因设备未上电、接口超时或采集抑制而无效。现有方案或省略有效性信息，或将有效标志分散写入各遥测项内部字段，解析规则不统一。

**5. 循环覆盖与绝对时间脱节**

循环覆盖常依赖写满标志或记录计数。按绝对时间查询时，难以直接判定目标时段是否已被覆盖，亦难以由时间推知覆盖边界。

**（三）现有技术未能解决的技术问题归纳**

现有技术——包括最接近的CN115664490B——虽已利用时间信息辅助寻址，但仍以**当前写地址／写指针**为寻址基准，属于相对寻址。其未能解决的核心技术问题是：

如何使历史遥测记录的存储槽位由绝对时标唯一确定，使写入、按时间段检索及崩溃复位后的继续写入均**不依赖当前写地址或写指针元数据**，并在定长多文件组织下统一表达遥测项有效性。

---

## 四、发明/实用新型内容

### 1. 要解决的技术问题

本发明要解决的技术问题是：克服以当前写地址为基准的相对寻址历史遥测存储方案在崩溃复位后须恢复写指针、写入与检索均依赖写指针状态的缺陷，提供一种由北斗星时绝对确定存储槽位的历史遥测快速存储检索方法，使写入、按时间段检索及复位后继续写入均不依赖当前写地址。

具体包括：

（1）如何建立北斗星时秒数到存储槽位（文件号、记录偏移）的唯一、可逆映射，使存储位置由绝对时标直接确定；

（2）如何以定长多文件组织支撑上述绝对映射，并统一表达时间戳、遥测项数据与遥测项有效性；

（3）如何在不维护写指针元数据的条件下完成循环覆盖；

（4）如何在系统崩溃复位后，仅依据当前北斗星时恢复正确写入位置。

### 2. 为解决技术问题而采用的技术方案

**技术方案描述：**

本发明提供一种基于北斗星时绝对定位的历史遥测快速存储检索方法，其区别于最接近现有技术CN115664490B的核心在于：采用**绝对槽位映射**——任一北斗星时秒数T在给定采集周期P、文件数n、单文件记录数m下，唯一对应一个存储槽位(F,R)；写入地址与检索地址的计算**均不引用当前写地址**；系统崩溃复位后**不恢复写指针**，直接由当前北斗星时重新计算槽位。

技术方案包括以下相互配合的技术手段。

**（一）定长多文件循环存储空间**

在星务平台非易失性存储介质上创建n个历史遥测文件，文件名按整数0、1、…、n−1顺序编号，文件编号取值范围为闭区间[0, n−1]。每个历史遥测文件预分配固定容量，容纳相同个数m条定长历史遥测记录。n与m为正整数，在星务软件配置中预先设定并在轨保持不变。

定义循环存储空间总容量 N＝n×m。对任一逻辑序号S：

- 文件号 F＝(S div m) mod n；
- 记录偏移 R＝S mod m；
- 文件内字节起始偏移 Addr＝R×Len；

其中 div 表示整数除法，mod 表示取模，Len为单条历史遥测记录固定字节长度。

上述组织的作用：将连续逻辑序号均匀映射到有限文件集合，为绝对时标到物理位置的换算提供确定性几何结构；无需索引表。

**（二）绝对槽位映射规则（本发明区别技术特征之一）**

设采集周期为P秒，P为正整数。对北斗星时秒数T，定义：

S＝T div P， R＝S mod m， F＝(S div m) mod n。

**绝对性含义：** 给定配置参数(n,m,P)后，函数 f(T)＝(F,R) 为单值映射；计算过程的输入仅为T及配置参数，**不包含当前写地址、当前文件号或当前偏移**。因此：

- 写入时：由本周期北斗星时秒数直接得到写入槽位；
- 检索时：由查询时刻北斗星时秒数直接得到读取槽位；
- 复位后：由当前北斗星时秒数直接得到应写入槽位。

这与CN115664490B中“以ADsd为基准的相对地址计算”形成本质区别。

进一步地，将32位定位信息划分为四个位域：

```
| 循环代次位域 | 文件号位域 | 记录偏移位域 | 采集周期位域 |
```

在一种实施方式中，位宽依次为6比特、8比特、16比特、2比特，对应：

- n＝256，m＝65536，N＝16,777,216；
- 当P＝4秒时，保存窗口 Tspan＝N×P＝67,108,864秒，合776天；
- 循环代次 G＝(S div N) mod 64，用于区分相隔整倍N后的相同(F,R)。

当S跨越N的整数倍后，f(T)回到已占用槽位，新记录覆盖旧记录，形成由绝对时标自然驱动的循环覆盖，无需单独的写满标志或覆盖调度状态机。

**（三）三字段定长历史遥测记录格式（本发明区别技术特征之二）**

每条历史遥测记录固定长度Len，由依次排列的三部分构成：

```
┌────────────────┬─────────────────────────┬──────────────────────────────┐
│ 时间戳（4字节） │ 遥测项数据区（DataLen字节）│ 遥测项掩码（ItemNum字节）      │
└────────────────┴─────────────────────────┴──────────────────────────────┘
```

（1）时间戳：4字节，存储本记录对应的北斗星时秒数T（32位无符号整数），用于检索后的一致性校验及覆盖代次识别；

（2）遥测项数据区：定长DataLen字节，按预先约定顺序存放各遥测项原始采样数据；

（3）遥测项掩码：定长ItemNum字节，ItemNum等于遥测项个数；第i字节与第i个遥测项对应，取有效标志表示该遥测项本记录有效，取无效标志表示无效。

Len＝4＋DataLen＋ItemNum。定长保证 Addr＝R×Len 可直接计算。遥测项掩码与数据区分离，保证无效遥测项在检索与下传中被准确剔除。

**（四）不依赖写指针的写入步骤**

步骤W1：读取当前北斗星时秒数T；

步骤W2：仅依据T及配置(n,m,P)计算F、R，**不读取、不更新任何写指针元数据**；

步骤W3：组装定长历史遥测记录：时间戳写入T；遥测项数据区写入本周期采样值；遥测项掩码按各遥测项是否有效填写；

步骤W4：打开文件号为F的历史遥测文件，定位至Addr＝R×Len，写入Len字节并刷写。

**（五）不依赖写指针的按时间段检索步骤**

设查询时间段为[Tstart, Tend]：

步骤Q1：若查询跨度超过保存窗口Tspan，将有效下界对齐为Tend对应窗口起点，以保证仅检索仍可能存在的记录；

步骤Q2：计算Sstart＝Tstart div P，Send＝Tend div P；

步骤Q3：对Si从Sstart到Send，计算(Fi,Ri)，读取对应记录；

步骤Q4：**时间戳一致性校验**：若记录内时间戳等于Si×P（或等于该槽位当前代次应有的时间），则纳入结果集；否则判定该槽位已被更新代次覆盖或尚未写入，标记缺失；

步骤Q5：按遥测项掩码输出有效遥测项。

起止槽位均由绝对时标直接计算，定位复杂度为常数阶；顺序读取复杂度与结果集大小成线性关系。

**（六）崩溃复位免写指针恢复（本发明区别技术特征之三）**

系统崩溃复位后，星务软件重新获取当前北斗星时秒数Tcurr，执行与步骤W2相同的绝对槽位计算，得到当前应写入的(F,R)并继续写入。**全程不恢复、不回读“当前文件号”“当前偏移”“当前写地址ADsd”等状态。**

若复位打断某次刷写，该槽位可能残留旧代次数据；后续检索通过步骤Q4的时间戳一致性校验识别空洞，保证结果正确性。

**术语定义：**

- 北斗星时秒数：北斗卫星导航系统时标对应的整秒计数，由星务平台时间管理模块提供；
- 绝对槽位映射：仅以绝对时标及固定配置参数确定存储位置、不引用当前写地址的映射规则；
- 存储槽位：由文件号F与记录偏移R唯一标识的定长记录位置；
- 写指针元数据：用于记录当前写入位置的状态数据，包括但不限于当前写地址、当前文件号、当前记录偏移；
- 遥测项掩码：标识各遥测项在本记录中是否有效的字节序列。

### 3. 与最接近现有技术的区别特征及非显而易见性说明

相对CN115664490B，本发明的区别技术特征为：

| 对比维度 | CN115664490B | 本发明 |
|---|---|---|
| 寻址基准 | 当前写地址ADsd（相对寻址） | 北斗星时秒数T（绝对槽位映射） |
| 写入位置确定 | 依赖持续维护的写指针／当前地址 | 仅由T计算(F,R)，无写指针 |
| 复位后行为 | 须恢复ADsd或重建写位置 | 直接由Tcurr计算槽位，免恢复 |
| 记录组织 | 按速率连续写入FLASH流式空间 | n×m定长多文件＋三字段定长记录 |
| 有效性表达 | 未单独规定遥测项级掩码 | 定长遥测项掩码与数据区分离 |
| 覆盖判定 | 依赖写满后回绕写指针 | 逻辑序号对N取模，由时标自然驱动 |

上述区别特征相互配合产生的技术效果——**写入／检索／复位三路径均消除写指针依赖**——并非CN115664490B与常规环形缓冲简单叠加所能直接得到：常规环形缓冲仍需维护写指针；CN115664490B的时间换算亦以写指针为基准。本领域技术人员在面对“复位后写位置丢失”这一技术问题时，由相对寻址出发通常会选择加固元数据落盘或扫描重建，而不会转而放弃写指针、改为绝对时标唯一确定槽位。因此本发明具备创造性。

### 4. 在本发明技术方案基础上的改进与变形

**技术方案的进一步改进和变形：**

（1）位域位宽变形：在保持绝对槽位映射的前提下，重新分配循环代次、文件号、记录偏移、采集周期各位域位宽。

（2）采集周期编码扩展：采集周期位域编码P＝1秒、2秒、4秒、8秒等离散取值。

（3）文件命名变形：顺序编号增加固定前缀或后缀，例如HistTM_0至HistTM_(n−1)。

（4）遥测项掩码位宽变形：由每遥测项1字节变形为每遥测项1比特的位图并按字节对齐，记录仍保持定长。

（5）双区冗余变形：同一逻辑序号在两套独立文件集合中各写一份，读取时交叉校验。

（6）下传打包变形：检索结果经遥控回放通道或延时遥测通道下传。

（7）装置与介质变形：将方法固化为含处理器与非易失性存储器的星上装置，或存储为计算机程序。

### 5. 有益效果

**有益效果：**

（1）消除写指针单点故障：存储槽位由北斗星时绝对确定，写入与检索均不依赖当前写地址，从根源上避免写指针丢失、损坏或不一致导致的存储失效。

（2）崩溃复位路径缩短：复位后无需扫描存储区、无需回读元数据重建ADsd，直接由当前北斗星时定位，缩短星务软件恢复路径，提高在轨可用性。

（3）检索定位为常数阶：查询起止时刻经同一映射直接得到起止槽位，避免索引查找与全文件扫描。

（4）循环覆盖与绝对时间一致：保存窗口由N与P唯一确定；在所述位宽配置及P＝4秒条件下为67,108,864秒（776天）；覆盖边界可由时标直接推知。

（5）遥测项有效性可判定：定长遥测项掩码保证无效数据不被误用。

（6）相对最接近现有技术的对比效果：在同等复位条件下，相对寻址方案须完成写指针恢复后方可继续写入；本发明在获得北斗星时后即可继续写入，恢复步骤数减少为“授时→槽位计算→写入”三步。

---

## 五、附图及附图说明

**附图及说明：**

**附图1：本发明绝对槽位映射总体流程图**

```
创建n个定长历史遥测文件(0..n-1)
            │
            ▼
   读取北斗星时秒数T（绝对时标）
            │
            ▼
   S=T div P
   R=S mod m；F=(S div m) mod n
   ※ 计算不引用当前写地址 ※
            │
            ▼
   组装：时间戳|数据区|遥测项掩码
   写入文件F偏移R×Len
            │
            ▼
   查询/复位：同样仅由时标计算槽位
```

附图标记：绝对时标T；逻辑序号S；文件号F；记录偏移R；遥测项掩码。

**附图2：与最接近现有技术的寻址对比示意图**

```
相对寻址（CN115664490B）：
  回放地址 = ADsd - (Tsd - Tq) × 速率
             ▲
             └── 必须已知当前写地址ADsd

绝对槽位映射（本发明）：
  (F,R) = f(T; n,m,P)
             ▲
             └── 仅依赖绝对时标T与固定配置
```

**附图3：定长历史遥测记录格式示意图**

```
| 时间戳4B | 遥测项数据区DataLen | 遥测项掩码ItemNum |
Len = 4 + DataLen + ItemNum
```

**附图4：32位定位位域划分示意图**

```
|循环代次6bit|文件号8bit|记录偏移16bit|采集周期2bit|
```

**附图5：崩溃复位免写指针恢复对比流程图**

```
相对寻址复位：丢失ADsd → 扫描/回读元数据 → 重建写指针 → 继续写入
本发明复位：  获取Tcurr → 计算(F,R) → 继续写入
```

---

## 六、具体实施方式

**具体实施方案如下：**

本实施方式以低轨卫星星务平台为应用对象。设定：

- n＝256，文件名0…255；m＝65536；P＝4秒；
- ItemNum＝128；DataLen＝256；Len＝388；
- N＝16,777,216；Tspan＝67,108,864秒（776天）；
- 位域｜6bit｜8bit｜16bit｜2bit｜，采集周期编码取表示P＝4秒的预定值；
- 星务软件中**不分配写指针元数据存储区**。

**实施步骤一：历史遥测文件初始化**

创建256个历史遥测文件，各预分配65536×388字节，初始化为全零。初始化完成后不写入任何写指针。

**实施步骤二：绝对槽位写入**

每4秒执行：

（1）读取北斗星时秒数T；

（2）S＝T div 4，R＝S mod 65536，F＝(S div 65536) mod 256；

（3）组装388字节记录（时间戳＝T，数据区＝本周期采样，掩码＝各遥测项有效标志）；

（4）写入文件F偏移R×388并刷写。

**实施步骤三：按时间段检索与一致性校验**

对[Tstart, Tend]计算Sstart、Send，依次读取；仅当记录时间戳等于Si×4时纳入结果，并按掩码输出有效遥测项。

**实施步骤四：崩溃复位对比验证（体现区别技术特征）**

工况A（对照，相对寻址）：维护当前写地址ADsd；在写入刷写前注入复位，清除易失性内存中的ADsd。恢复时须扫描文件或回读元数据重建ADsd，扫描耗时随已用容量增长。

工况B（本发明）：不维护ADsd；同样注入复位。恢复后读取Tcurr，直接计算(F,R)并继续写入。恢复路径固定为授时→计算→写入，不随已用容量增长。

验证指标：复位后首次成功写入的耗时、写入槽位与Tcurr的一致性、后续按时间段检索的完整率。工况B在获得有效北斗星时后即可恢复写入；工况A须完成写指针重建。

**实施步骤五：数值验证**

T＝1,000,000时：S＝250,000，R＝23424，F＝3，字节偏移＝23424×388。写入与以该时刻为查询点的检索定位一致。

**实施步骤六：装置实施方式**

一种星上历史遥测存储检索装置，包括处理器、北斗授时接口、非易失性存储器。非易失性存储器存储所述n个历史遥测文件，且**不依赖独立写指针元数据区**；处理器执行上述绝对槽位映射的写入与检索步骤。

---

## 七、技术创新点概括

本方案重点要保护如下几方面的创新点：

1）**北斗星时绝对槽位映射**：以S＝T div P、F＝(S div m) mod n、R＝S mod m建立绝对时标到存储槽位的唯一映射，写入与检索均不引用当前写地址，区别于CN115664490B的相对寻址。

2）**崩溃复位免写指针恢复**：复位后仅依据当前北斗星时重新计算存储槽位并继续写入，无需恢复当前写地址、当前文件号或当前偏移。

3）**定长多文件循环存储组织**：n个顺序编号文件、每文件m条定长记录，为绝对映射提供确定性几何结构，并以逻辑序号对N取模实现时标驱动的循环覆盖。

4）**三字段定长记录＋时间戳一致性校验**：时间戳｜遥测项数据区｜遥测项掩码；检索时校验时间戳以识别覆盖代次与空洞。

5）**遥测项级有效性掩码**：与数据区分离的定长掩码，保证无效遥测项可判定。

---

## 八、权利要求书

**权利要求1.** 一种基于北斗星时绝对定位的历史遥测快速存储检索方法，应用于低轨卫星星务平台，其特征在于，包括：

在星务平台非易失性存储介质上创建n个历史遥测文件，文件名按0至n−1顺序编号，每个历史遥测文件容纳m条固定长度Len的历史遥测记录，n与m为正整数；循环存储空间总容量N＝n×m；

每条历史遥测记录由依次排列的时间戳、遥测项数据区与遥测项掩码构成；所述时间戳长度为4字节，存储北斗星时秒数；所述遥测项数据区长度为DataLen字节；所述遥测项掩码长度为ItemNum字节，ItemNum等于遥测项个数，掩码中每一字节与一个遥测项对应并标识该遥测项在本记录中是否有效；Len＝4＋DataLen＋ItemNum；

建立绝对槽位映射：对北斗星时秒数T与采集周期P，计算逻辑序号S＝T div P、记录偏移R＝S mod m、文件号F＝(S div m) mod n；所述绝对槽位映射的计算输入仅为T及配置参数n、m、P，不引用当前写地址、当前文件号或当前记录偏移；

按采集周期执行写入：读取当前北斗星时秒数，经所述绝对槽位映射得到F与R，将组装后的历史遥测记录写入文件号为F的历史遥测文件中字节偏移为R×Len的位置；当S跨越N的整数倍时，新记录覆盖该槽位旧记录；

按时间段检索时，对查询起止北斗星时秒数分别执行所述绝对槽位映射以确定起止槽位并读取记录；

星务平台崩溃复位后，重新获取当前北斗星时秒数，经所述绝对槽位映射直接确定当前存储槽位并继续写入，不恢复写指针元数据。

**权利要求2.** 根据权利要求1所述的方法，其特征在于，按时间段检索还包括：对读取的历史遥测记录执行时间戳一致性校验，当记录内时间戳与由逻辑序号还原的北斗星时秒数一致时纳入结果集，否则标记该槽位数据缺失；并依据遥测项掩码输出有效遥测项数据。

**权利要求3.** 根据权利要求1所述的方法，其特征在于，用于表达所述绝对槽位映射的32位信息划分为循环代次位域、文件号位域、记录偏移位域与采集周期位域；所述文件号位域位宽为8比特且n＝256，所述记录偏移位域位宽为16比特且m＝65536，所述采集周期位域位宽为2比特，所述循环代次位域位宽为6比特。

**权利要求4.** 根据权利要求3所述的方法，其特征在于，所述采集周期P＝4秒，N＝16,777,216，保存窗口为67,108,864秒。

**权利要求5.** 根据权利要求1所述的方法，其特征在于，历史遥测写入具体包括：

读取当前北斗星时秒数T；

仅依据T及配置参数n、m、P计算F与R；

将T、本周期遥测项数据及遥测项掩码组装为长度为Len的历史遥测记录；

在文件号为F的历史遥测文件中自字节偏移R×Len处写入所述历史遥测记录；

写入过程不更新写指针元数据。

**权利要求6.** 根据权利要求1所述的方法，其特征在于，所述星务平台不在非易失性存储介质中维护用于记录当前写入位置的写指针元数据区。

**权利要求7.** 根据权利要求1所述的方法，其特征在于，所述遥测项掩码采用每遥测项对应1比特的位图并按字节对齐填充，历史遥测记录仍保持固定长度Len。

**权利要求8.** 一种星上历史遥测存储检索装置，其特征在于，包括处理器、北斗授时接口与非易失性存储器；所述非易失性存储器中存储权利要求1所述的n个历史遥测文件；所述处理器执行权利要求1至7中任一项所述方法的步骤；所述装置不依赖独立写指针元数据确定当前写入位置。

**权利要求9.** 一种计算机可读存储介质，其上存储有计算机程序，其特征在于，所述计算机程序被处理器执行时实现权利要求1至7中任一项所述方法的步骤。
